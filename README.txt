to commit you need to build the project first 

always replace the created index.html from unity with the index.html in the OG HTML GAME Folder
always put the updated working version of the html into the folder to have them ready 